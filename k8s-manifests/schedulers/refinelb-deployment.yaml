---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: refinelb-scheduler
  namespace: kube-system
  labels:
    app: refinelb-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: refinelb-scheduler
  template:
    metadata:
      labels:
        app: refinelb-scheduler
    spec:
      serviceAccountName: custom-scheduler
      nodeSelector:
        node-role.kubernetes.io/control-plane: ""
      containers:
        - name: scheduler
          image: refinelb-scheduler:latest
          imagePullPolicy: Never
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          env:
            - name: SCHEDULER_NAME
              value: "refinelb-scheduler"
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
